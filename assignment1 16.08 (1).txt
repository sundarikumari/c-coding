#include <stdio.h>

int main(void) {
    int a[4];
    for(int i=0;i<4;i++)
    {
        scanf("%d",&a[i]);
    }
    int sum=0;
    for(int i=0;i<4;i++)
    {
        if(a[i]>=10)
        {
            sum+=1;
        }
    }
    printf("%d",sum);
	return 0;
}
